# Output folder

This is simply a placeholder file to force the creation of the `out` folder so it syncs through `git`. This avoids the issue of Python not finding the `out` folder after cloning from GitHub.